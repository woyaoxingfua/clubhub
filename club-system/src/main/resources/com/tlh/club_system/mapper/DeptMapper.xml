<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tlh.club_system.mapper.DeptMapper">

    <resultMap id="BaseResultMap" type="com.tlh.club_system.entity.Dept">
            <id property="deptId" column="dept_id" />
            <result property="deptName" column="dept_name" />
            <result property="createTime" column="create_time" />
    </resultMap>

    <sql id="Base_Column_List">
        dept_id,dept_name,create_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from sys_dept
        where  dept_id = #{deptId} 
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from sys_dept
        where  dept_id = #{deptId} 
    </delete>
    <insert id="insert" keyColumn="dept_id" keyProperty="deptId" parameterType="com.tlh.club_system.entity.Dept" useGeneratedKeys="true">
        insert into sys_dept
        ( dept_id,dept_name,create_time)
        values (#{deptId},#{deptName},#{createTime})
    </insert>
    <insert id="insertSelective" keyColumn="dept_id" keyProperty="deptId" parameterType="com.tlh.club_system.entity.Dept" useGeneratedKeys="true">
        insert into sys_dept
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="deptId != null">dept_id,</if>
                <if test="deptName != null">dept_name,</if>
                <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="deptId != null">#{deptId},</if>
                <if test="deptName != null">#{deptName},</if>
                <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.tlh.club_system.entity.Dept">
        update sys_dept
        <set>
                <if test="deptName != null">
                    dept_name = #{deptName},
                </if>
                <if test="createTime != null">
                    create_time = #{createTime},
                </if>
        </set>
        where   dept_id = #{deptId} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.tlh.club_system.entity.Dept">
        update sys_dept
        set 
            dept_name =  #{deptName},
            create_time =  #{createTime}
        where   dept_id = #{deptId} 
    </update>
</mapper>
