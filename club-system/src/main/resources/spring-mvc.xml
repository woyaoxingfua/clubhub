<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 加载AI配置文件 -->
    <context:property-placeholder location="classpath:ai.properties" ignore-unresolvable="true"/>

    <!-- 扫描Controller -->
    <context:component-scan base-package="com.tlh.club_system.controller"/>
    
    <!-- 扫描配置类 -->
    <context:component-scan base-package="com.tlh.club_system.config"/>
    
    <!-- 扫描拦截器组件 -->
    <context:component-scan base-package="com.tlh.club_system.interceptor"/>

    <!-- ObjectMapper 工厂Bean，注册 JSR-310 并设置日期格式 -->
    <bean id="jacksonObjectMapper" class="org.springframework.http.converter.json.Jackson2ObjectMapperFactoryBean">
        <property name="serializationInclusion" value="NON_NULL"/>
        <property name="simpleDateFormat" value="yyyy-MM-dd HH:mm:ss"/>
        <property name="modules">
            <list>
                <bean class="com.fasterxml.jackson.datatype.jsr310.JavaTimeModule"/>
            </list>
        </property>
    </bean>

    <!-- 开启MVC注解驱动 (自动配置Jackson等) -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>text/plain;charset=UTF-8</value>
                        <value>text/html;charset=UTF-8</value>
                    </list>
                </property>
            </bean>
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <property name="objectMapper" ref="jacksonObjectMapper"/>
                <property name="supportedMediaTypes">
                    <list>
                        <value>application/json;charset=UTF-8</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- 文件上传解析器 (必须配置才能处理multipart请求) -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.support.StandardServletMultipartResolver"/>

    <!-- 静态资源映射 (文件上传访问) -->
    <mvc:resources mapping="/uploads/**" location="file:${user.home}/club-system-uploads/"/>

    <!-- 静态资源处理 (交给默认Servlet) -->
    <mvc:default-servlet-handler/>
    
    <!-- 拦截器配置 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/api/**"/>
            <mvc:exclude-mapping path="/api/auth/login"/>
            <mvc:exclude-mapping path="/api/auth/register"/>
            <mvc:exclude-mapping path="/api/dept/list"/>
            <mvc:exclude-mapping path="/api/club/list"/>
            <mvc:exclude-mapping path="/api/club/detail/**"/>
            <mvc:exclude-mapping path="/api/event/list"/>
            <mvc:exclude-mapping path="/api/notice/list"/>
            <bean class="com.tlh.club_system.interceptor.UserInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 视图解析器 (虽然主要做REST API，但保留JSP支持是个好习惯) -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

</beans>
